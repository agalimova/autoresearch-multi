"""
autoresearch engine — the core experiment loop.

Modules:
    experiment   — agent logic, hyperparameter search, training
    findings     — Finding dataclass + JSON persistence
    prompts      — cross-pollination prompt assembly
    semantic_diff — sem CLI wrapper
    anonymize    — strip sem output for private_anon visibility
"""
